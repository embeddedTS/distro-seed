#!/bin/bash -e

cd "${DS_TASK_WORK}"

mkdir build
cd build
cmake .. -DCMAKE_INSTALL_PREFIX="${DS_OVERLAY}/usr" -DCMAKE_C_FLAGS="-L${DS_TASK_WORK_DS_COMPONENT_LIBLVGL}/build/lib -I${DS_TASK_WORK_DS_COMPONENT_LIBLVGL} -I${DS_TASK_WORK}" -DCMAKE_CXX_FLAGS="-L${DS_TASK_WORK_DS_COMPONENT_LIBLVGL}/build/lib -I${DS_TASK_WORK_DS_COMPONENT_LIBLVGL} -I${DS_TASK_WORK}" -DBUILD_SHARED_LIBS=TRUE -DCMAKE_TOOLCHAIN_FILE="${CMAKE_CROSS}"
make install
