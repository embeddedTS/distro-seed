#!/bin/bash -e

SOURCE="$DS_STAGING"

cd "$SOURCE"

mkdir build
cd build
cmake .. -DCMAKE_INSTALL_PREFIX="${DS_OVERLAY}/usr" -DCMAKE_C_FLAGS="-L${DS_STAGING_DS_COMPONENT_LIBLVGL}/build/lib -L${DS_STAGING_DS_COMPONENT_LV_DRIVERS}/build/lib -I${DS_STAGING_DS_COMPONENT_LIBLVGL} -I${DS_STAGING_DS_COMPONENT_LV_DRIVERS}" -DCMAKE_CXX_FLAGS="-L${DS_STAGING_DS_COMPONENT_LIBLVGL}/build/lib -L${DS_STAGING_DS_COMPONENT_LV_DRIVERS}/build/lib -I${DS_STAGING_DS_COMPONENT_LIBLVGL} -I${DS_STAGING_DS_COMPONENT_LV_DRIVERS}" -DCMAKE_TOOLCHAIN_FILE="${CMAKE_CROSS}"
make install
